# Changesets

This directory uses [Changesets](https://github.com/changesets/changesets) to manage versions and changelogs.

Add a changeset for your PR:

```bash
pnpm changeset
```

Pick the packages affected and the type of change (patch/minor/major).
